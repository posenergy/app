import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    view: {
      width: '100%',
      alignItems: 'center',
      alignSelf: 'center',
      justifyContent: 'center',
    },
})
